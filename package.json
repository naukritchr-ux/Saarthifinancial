{
  "name": "saarthi-finance-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd fintective-project-main/frontend && npm install && npm run build",
    "vercel-build": "cd fintective-project-main/frontend && npm install && npm run build"
  }
}
