# Server Configuration
PORT=5000

# Database Configuration (MySQL)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=saarthi_finance
DB_PORT=3306

# Secret key for token verification (if authentication is used)
JWT_SECRET=your_jwt_secret_key_here
