{
  "framework": "vite",
  "buildCommand": "cd tds-ui && npm install && npm run build",
  "outputDirectory": "tds-ui/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
